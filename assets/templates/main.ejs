<script type='text/x-underscore-template' id='main-view'>
    <div class='reader'>
    <% _.each(items.reverse(), function(item){%>
        <% if (item.visible && item.filter){ %>
            <div class='unread-item <%=
                    item.read ? 'read' : ''
                %> <%= 
                    item.selected ? 'selected' : '' 
                %>' data-id='<%= item.feedItemId %>'>
                <a class='item-link <%= item.interesting ? 'interesting' : ''%>' href='<%= item.link %>' target='_blank'>
                    <%= item.title.replace(/&amp;&#35;40;/g, '&#40;') %>
                </a>
                <span><%= item.summary %></span>
            </div>
        <% } %>
    <% }) %>
    <% if(loading) { %>
        Loading...
    <% } else if(items.length===0){ %>
        None
    <% } %>
    </div>
</script>